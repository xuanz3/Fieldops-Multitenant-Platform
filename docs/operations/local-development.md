# Local Development

## Required tools

- .NET 10 SDK
- Node.js and npm
- Docker Desktop or another Docker-compatible runtime for PostgreSQL

## Safety

Use only values from `.env.example` for local fictional data. Never commit `.env`, cloud credentials, tokens or real customer information.
